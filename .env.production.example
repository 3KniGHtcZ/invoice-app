# =============================================================================
# Production Environment Variables Example
# =============================================================================
# Copy this file to .env.production and fill in your actual values
# NEVER commit the actual .env.production file to git!

# =============================================================================
# Azure AD Configuration (REQUIRED)
# =============================================================================
# Get these values from Azure Portal > App Registrations > Your App
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret
AZURE_TENANT_ID=consumers

# =============================================================================
# Application URLs
# =============================================================================
# Frontend URL - where your application is accessible
# This is used for OAuth redirects and CORS
FRONTEND_URL=https://yourdomain.com

# OAuth Redirect URI - must match Azure AD app registration
# Format: https://yourdomain.com/api/auth/callback
REDIRECT_URI=https://yourdomain.com/api/auth/callback

# Allowed origins for CORS (comma-separated)
# Include all domains that should be able to access the API
ALLOWED_ORIGINS=https://yourdomain.com

# =============================================================================
# Session Configuration (REQUIRED)
# =============================================================================
# Generate a secure random string (64 characters recommended)
# You can generate one with: openssl rand -hex 64
SESSION_SECRET=your-very-long-random-secret-key-here

# Cookie domain for production (optional)
# Use .yourdomain.com to share cookies across subdomains
# Leave empty for single domain or localhost
COOKIE_DOMAIN=.yourdomain.com

# Cookie security - set to true for HTTPS, false for HTTP (localhost)
# ALWAYS use true in production with HTTPS
# Use false for local testing on http://localhost
COOKIE_SECURE=true

# =============================================================================
# Google Gemini AI (REQUIRED for invoice extraction)
# =============================================================================
# Get API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# =============================================================================
# Discord Notifications (OPTIONAL)
# =============================================================================
# Discord webhook for invoice notifications
# Get webhook URL from Discord: Server Settings > Integrations > Webhooks
# Format: https://discord.com/api/webhooks/WEBHOOK_ID/WEBHOOK_TOKEN
DISCORD_WEBHOOK_URL=

# Watchtower Auto-Update Notifications (OPTIONAL)
# Discord notifications when Watchtower updates the container
# Format: discord://WEBHOOK_TOKEN@WEBHOOK_ID
# Example: If webhook is https://discord.com/api/webhooks/123456789/abcdefgh
# Then use: discord://abcdefgh@123456789
WATCHTOWER_NOTIFICATION_URL=

# =============================================================================
# Node.js Configuration
# =============================================================================
NODE_ENV=production
PORT=3000

# =============================================================================
# IMPORTANT NOTES:
# =============================================================================
# 1. Azure AD Redirect URI:
#    - Must be registered in Azure Portal > App Registrations > Authentication
#    - Add both: https://yourdomain.com/api/auth/callback
#
# 2. Generate SESSION_SECRET:
#    Run: openssl rand -hex 64
#
# 3. CORS Configuration:
#    - Make sure FRONTEND_URL is in ALLOWED_ORIGINS
#    - For multiple origins use: https://domain1.com,https://domain2.com
#
# 4. Cookie Domain:
#    - Only set if using subdomains (e.g., api.yourdomain.com, app.yourdomain.com)
#    - Leave empty if frontend and backend are on same domain
