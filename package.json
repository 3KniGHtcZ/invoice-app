{
  "name": "invoice-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx}": [
      "cd frontend && yarn lint --fix"
    ],
    "backend/**/*.{ts}": [
      "cd backend && yarn lint --fix"
    ]
  }
}
